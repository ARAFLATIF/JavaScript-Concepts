Overview
setTimeout and setInterval are methods used to execute functions after a specified amount of time or at regular intervals.
setTimeout
The setTimeout method executes a function once after a specified delay.

  setTimeout(function() {
    console.log("This will be executed after 2 seconds");
}, 2000);


setInterval
The setInterval method executes a function at regular intervals.

  setInterval(function() {
    console.log("This will be executed every 2 seconds");
}, 2000);

